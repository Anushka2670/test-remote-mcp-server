[project]
name = "testremoteserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anushka2670", email = "anushkabhagat8032@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=4.0.5",
]

[project.scripts]
testremoteserver = "testremoteserver:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
